# Skribbl Clone - Server

## Requirements
- Node.js 18+
- Redis (default at redis://localhost:6379) or set REDIS_URL env var

## Run
cd server
npm install
node index.js

The server listens on port 4000 by default.
